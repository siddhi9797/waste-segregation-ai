# TODO - Continue button on History chat

- [ ] Gather understanding of current History UI and existing chat flow
- [ ] Update `frontend/src/pages/History.jsx` to add a per-question **Continue** button
- [ ] On clicking **Continue**, render an **Ask AI** mini-chat (textarea/input + Ask AI button)
- [ ] Wire the mini-chat to existing backend `/api/waste/chat` using the selected `wasteId` + question
- [ ] Update UI to display new AI answers and keep them inside the selected chat thread
- [ ] (Optional) Add minimal styling (inline or `frontend/src/styles/...`) for the continue panel
- [ ] Run frontend lint/build (if available) or quick dev check

